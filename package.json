{
  "name": "quicksilver-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=ai-example",
    "build": "npm run build --workspaces",
    "start": "npm run start --workspace=ai-example"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
